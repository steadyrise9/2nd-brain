# Second Brain — Lite kernel dependencies
#
# The kernel aims to stay close to pure Python. Capabilities beyond the core
# (search, embeddings, OCR, audio, integrations, Telegram, …) are installed from
# the store, and each store plugin declares its own extra dependencies.

# --- LLM clients (service_llm) ---
openai
lmstudio
litellm

# --- HTTP ---
requests

# --- File watcher (pipeline/watcher) ---
watchdog

# --- Images: attachment handling + optional embedded-image extraction ---
Pillow

# --- Optional: richer document text extraction (parse_text) ---
# Plaintext and code always work with zero extra deps. The PDF/Word/PowerPoint
# parsers degrade gracefully when these are absent — install to enable them:
# PyMuPDF
# python-docx
# python-pptx

# ===================================================================
# Moved to store plugins — install alongside the plugin that needs them:
#   search / embeddings : sentence-transformers, numpy, torch
#   audio transcription : faster-whisper, torch
#   ocr                 : easyocr, torch; winsdk (win); pyobjc-framework-Vision/Quartz (mac)
#   tabular / archives  : pandas, py7zr, rarfile
#   scheduling          : croniter, cron-descriptor
#   telegram frontend   : python-telegram-bot
#   google (gmail/drive): google-auth, google-auth-oauthlib, google-api-python-client
#   mcp client          : mcp
# ===================================================================
